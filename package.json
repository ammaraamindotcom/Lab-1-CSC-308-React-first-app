{
  "name": "csc-308-app",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
 "scripts": {
  "dev": "npm --workspace=packages/react-frontend run dev",
  "build": "npm --workspace=packages/react-frontend run build",
  "lint": "npm --workspace=packages/react-frontend run lint",
  "test": "echo \"Error: no test specified\" && exit 1"
 }
}
